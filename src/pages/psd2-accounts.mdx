import { Tab } from '@headlessui/react'
import Image from 'next/image'

export const description =
  'On this page, we'll dive into the Account Information Service Provider (AISP) component of the snappi PSD2 API.'

# Account Information Service Provider (AISP)

The Account Information Service provides account information services as an online service to provide consolidated information on one or more payment accounts held by a payment service user with one or more payment service provider(s). {{ className: 'lead' }}

<Note>
This API allows you to retrieve account information, balances and transactions from payment accounts that you have been granted access to.
</Note>

<Tab.Group>
  <Tab.List className="flex space-x-4 border-b mb-4">
    <Tab className={({ selected }) =>
      `px-4 py-2 text-sm font-medium rounded-t-lg ${
        selected 
          ? 'bg-white border-b-2 border-emerald-500 text-emerald-500'
          : 'text-gray-500 hover:text-gray-700'
      }`
    }>
      Account Information Flow
    </Tab>
    <Tab className={({ selected }) =>
      `px-4 py-2 text-sm font-medium rounded-t-lg ${
        selected 
          ? 'bg-white border-b-2 border-emerald-500 text-emerald-500'
          : 'text-gray-500 hover:text-gray-700'
      }`
    }>
      Account Consent Flow
    </Tab>
  </Tab.List>
  <Tab.Panels>
    <Tab.Panel>
      <div className="my-8">
        <Image 
          src="/PSD2-Accounts-API-flow.webp"
          alt="Account Information Flow"
          width={800}
          height={600}
          sizes="(max-width: 800px) 100vw, 800px"
        />
      </div>
    </Tab.Panel>
    <Tab.Panel>
      <div className="my-8">
        <Image 
          src="/accounts-api-flow.webp"
          alt="Account Consent Flow" 
          width={800}
          height={600}
          sizes="(max-width: 800px) 100vw, 800px"
        />
      </div>
    </Tab.Panel>
  </Tab.Panels>
</Tab.Group>

[Rest of the API documentation content remains unchanged]